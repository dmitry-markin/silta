# silta-session

Launcher and supervisor of one Silta session: runs Claude Code headless, reduces its output to a privacy-preserving journal log, rotates the session when its context grows past a size threshold.
